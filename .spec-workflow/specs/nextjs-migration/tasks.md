# Next.js 架构迁移 - 任务列表

- [x] 初始化 Next.js 配置（`package.json`、`next.config.mjs`、`tsconfig.json`、`next-env.d.ts`）。
- [x] 搭建 `app/layout.tsx` 与 `app/page.tsx`，复用现有 `App` UI。
- [x] 迁移并清理 Vite 相关入口与配置（`index.html`、`index.tsx`、`vite.config.ts`）。
- [x] 迁移全局样式到 `globals.css`，并通过 `next/script` 引入 Tailwind CDN。
- [x] 更新 `AGENTS.md`，补充 ADR/任务流程规则与 Next.js 架构规范。

