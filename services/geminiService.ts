// AI Functionality has been disabled to resolve build dependencies
export const getGeminiHint = async (
  levelContext: string,
  userQuery: string,
  logs: string[]
): Promise<string> => {
  return "通信模块离线：AI 辅助功能目前不可用。请仔细阅读任务描述和教程，依靠自己的技能完成任务。";
};