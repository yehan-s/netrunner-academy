# 游戏核心架构梳理 - 任务列表

- [x] 建立 `hooks/` 目录，并定义用于客户端逻辑封装的基本约定（仅在客户端组件中使用 Hook，避免副作用散落在各处）。
- [x] 将 DevTools 尺寸与拖拽逻辑从 `App.tsx` 抽离为独立 Hook，并在 `App` 中使用它，而不是直接操作全局事件。
- [x] 评估并规划网络模拟与关卡判定逻辑的拆分方案，并实现 `engine/networkEngine.ts` 作为集中管理模块。
